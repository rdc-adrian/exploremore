import { View, Text } from "react-native";

// Explore / Discover screen for browsing destinations and suggestions.
export default function ExploreScreen() {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text>Explore — discover destinations, recommendations, and deals</Text>
    </View>
  );
}
